function square(number) {
    return number * number;
  }
  // Anonymous function version:
  // const square = (number) => {
  //   return number * number;
  // }
  
  function add(number1, number2) {
    return number1 + number2;
  }
  // Anonymous function version:
  // const add = (number1, number2) => {
  //   return number * number;
  // }
  
  function subtract(number1, number2) {
    return number1 - number2;
  }
  // Anonymous function version:
  // const subtract = (number1, number2) => {
  //   return number - number;
  // }
  
  function multiply(number1, number2) {
    return number1 * number2;
  }
  // Anonymous function version:
  // const multiply = (number1, number2) => {
  //   return number * number;
  // }
  
  function divide(number1, number2) {
    return number1 / number2;
  }
  // Anonymous function version:
  // const divide = (number1, number2) => {
  //   return number / number;
  // }
  
  function madlib(noun1, adjective, noun2) {
    return `The ${ noun1 } ran to the ${ adjective } ${ noun2 }`;
  }
  // Anonymous function version:
  // const madlib = (noun1, adjective, noun2) => {
  //   return number * number;
  // }
  
  
  
  
  // Call the functions
  
  // console.log(square(3));
  
  // console.log(add(4, 19));
  
  // console.log(subtract(4, 19));
  
  // console.log(multiply(4, 19));
  
  // console.log(divide(4, 19));
  
  // console.log(madlib("dog", "green", "tent"));